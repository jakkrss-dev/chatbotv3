{
  "name": "rag-chatbot-v2-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && npx shx mkdir -p public && npx shx cp -r frontend/dist/. public/"
  }
}
